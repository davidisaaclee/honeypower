// Generated by CoffeeScript 1.9.2
(function() {
  var Model, Path, Trigger, Vector2, _,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  _ = require('lodash');

  Model = require('../Model');

  Vector2 = require('../graphics/Vector2');

  Path = require('../graphics/Path');


  /*
  Describes a "hotspot" which triggers an action when entered by an entity.
  
  Trigger ::=
     * The origin of this trigger's local coordinate system.
    position: Vector2
     * The shape of the trigger, in local space.
    shape: Path
     * The action to perform upon trigger activation.
    action: Function
   */

  Trigger = (function(superClass) {
    extend(Trigger, superClass);

    function Trigger() {
      return Trigger.__super__.constructor.apply(this, arguments);
    }

    Trigger.make = function(position, shape, action) {
      if (position == null) {
        position = Vector2.zero;
      }
      if (shape == null) {
        shape = Path.empty;
      }
      if (action == null) {
        action = (function() {});
      }
      return _.assign(new Trigger(), {
        position: position,
        shape: shape,
        action: action
      });
    };

    return Trigger;

  })(Model);

  module.exports = Trigger;

}).call(this);

//# sourceMappingURL=Trigger.js.map
