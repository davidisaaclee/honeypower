// Generated by CoffeeScript 1.9.2
(function() {
  var Entity, Scene, Set, _, addChildReducers, clamp, combineReducers, darko, defaultState, k, mapAssign, reducer, updeep, wrap;

  _ = require('lodash');

  updeep = require('updeep');

  darko = require('darko');

  combineReducers = require('redux').combineReducers;

  k = require('../ActionTypes');

  Scene = require('../model/Scene');

  Entity = require('../model/entities/Entity');

  Set = require('../util/Set');

  mapAssign = require('../util/mapAssign');

  addChildReducers = require('../util/addChildReducers');

  clamp = require('../util/clamp');

  wrap = require('../util/wrap');

  defaultState = Scene.empty;

  reducer = function(state, action) {
    if (state == null) {
      state = defaultState;
    }
    state = _.assign({}, state, {
      darko: darko.reducer(state.darko, action)
    });
    return state;
  };

  module.exports = reducer;

}).call(this);

//# sourceMappingURL=SceneReducer.js.map
